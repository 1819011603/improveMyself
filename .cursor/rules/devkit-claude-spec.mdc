---
description: 在本仓库工作时须阅读并遵守根目录 CLAUDE.md 中的项目规范
alwaysApply: true
---

# DevKit 项目执行规范

在本仓库内进行开发、重构、排查问题或回答与代码相关的问题时：

1. **必读上下文**：将仓库根目录的 `CLAUDE.md` 纳入上下文（例如在对话中使用 `@CLAUDE.md`），并以其为项目规范的唯一权威说明。
2. **按规范执行**：严格遵循 `CLAUDE.md` 中的技术栈、架构原则、代码风格、文件与目录组织、模块划分、终端与脚本执行交互、跨平台约定、数据与存储约定，以及提交信息格式。
3. **优先级**：若其他通用建议与 `CLAUDE.md` 不一致，以 `CLAUDE.md` 为准；仅当用户在当前对话中明确给出例外要求时，可按用户本次要求处理。

不在本规则文件中重复全文拷贝 `CLAUDE.md`，避免规范多处维护、内容漂移。
